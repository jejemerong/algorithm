#include <iostream>

using namespace std;

int targetNum, elementNum, result;
vector<int> targets, elements;

int main()
{
  freopen("input.txt", "r", stdin);
  cin << targetNum << endl;
  for(int  i = 0; i < targetNum; i++){
    cin >> targets[i];
  }

  return 0;
}
