#include <iostream>
#include <queue>
#include <stack>
using namespace std;

int v, e, start;
vector<vector<int> > graph;

#define INF = 9999999; 

void bfs(int &start, ) {

}

void dfs(int &start, ){

}

int main() {
    scanf("%d %d %d", &v, &e, &start);
    graph.resize(v);
    for(int i = 0; i < v; i++){

    }
    dfs(start, );
    bfs(start);
    return 0;
}